# Boost Consumable

Add a temporary boost item. On the main screen, add a "Use Boost" button that activates a 2x scrap generation multiplier for 60 seconds. Boosts cost 100 scrap to use. Display remaining boost time when active. Only one boost can be active at a time.
